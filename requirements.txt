# Bridge — Local Real-Time Voice-to-Voice Translation
# Generated for: RTX 3070 Ti (8 GB VRAM), Windows 11, Python 3.10+
#
# Install with:
#   uv pip install -r requirements.txt
# or:
#   pip install -r requirements.txt

# ── Web framework ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# ── STT ─────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.3
# CTranslate2 CUDA build (install separately if the above doesn't pull it):
#   pip install ctranslate2 --index-url https://download.pytorch.org/whl/cu121

# ── Audio I/O ────────────────────────────────────────────────────────────────
soundfile>=0.12.1
numpy>=1.26.0
# resampy>=0.4.3        # optional: higher-quality resampling (falls back to numpy)

# ── Translation ──────────────────────────────────────────────────────────────
httpx>=0.27.0
# ollama>=0.2.0         # optional: official Ollama Python client (replaces httpx calls)

# ── TTS — install ONE of the following ──────────────────────────────────────
kokoro-onnx>=0.4.0      # recommended on Windows (no PyTorch required)
# kokoro                # alternative: PyTorch-based, marginally higher quality
#
# ── CUDA support for faster-whisper (install separately if needed) ─────────
# pip install ctranslate2 --index-url https://download.pytorch.org/whl/cu121
